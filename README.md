# terraform-azure-keyvault-vm-nginx
Automated Azure infrastructure using Terraform. Provisions Azure Key Vault, securely stores VM credentials, assigns RBAC roles, deploys Linux VMs, retrieves secrets at runtime, and installs NGINX via Cloud-Init with a single terraform apply
